import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['cloudinary.images-iherb.com','res.cloudinary.com', 'farmaciard.com', 'i0.wp.com','pharmatech.sirv.com', 'spotseeker.sirv.com','upload.wikimedia.org'],
  },
};

export default nextConfig;
