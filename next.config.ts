import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['cloudinary.images-iherb.com', 'farmaciard.com', 'i0.wp.com','pharmatech.sirv.com'],
  },
};

export default nextConfig;
